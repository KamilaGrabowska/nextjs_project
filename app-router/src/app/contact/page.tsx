import {commonMetadata} from "@/common/shared-metadata";

export const metadata = {
    title: `Contact ${commonMetadata.title}`,
    description: "ths is contact page",
}

export default function ContactPage () {
    return (
        <div>
            <h1>Contact Page</h1>
        </div>
    );
}